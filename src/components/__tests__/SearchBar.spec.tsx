describe('<SearchBar />', () => {
  it('will fetch to see if any characters match the search query', () => {

  });

  it('will render a list of characters returned from the API', () => {

  });

  it('will fire the onFocus callback with the focus state', () => {

  });
})