describe('<ResultsDropdown />', () => {
  it('will render each of the characters names in the results prop', () => {

  })

  it('will execute the callback prop with the selected character when a character is selected', () => {

  })
})