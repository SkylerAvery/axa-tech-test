describe('<Navbar />', () => {
  it('will render a search bar', () => {

  })

  it('will darken the background when the searchbar is focused', () => {

  })

  it('will show a search query results dropdown when a search query is entered', () => {

  })

  it('will reset the background when the search bar is unfocused', () => {

  })

  it('will remove the results dropdown when the search query is unfocused', () => {

  })
})